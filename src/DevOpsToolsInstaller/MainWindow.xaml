<Window
    x:Class="DevOpsToolsInstaller.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls">

    <Grid x:Name="RootGrid" Background="{ThemeResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom title bar -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="48" Background="{ThemeResource SidebarBrush}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0" Spacing="10">
                <FontIcon Glyph="&#xE74C;" FontSize="18" Foreground="{ThemeResource AccentBrush}" />
                <TextBlock
                    Text="DevOps Tools Installer"
                    Foreground="{ThemeResource TextPrimaryBrush}"
                    FontWeight="SemiBold" />
                <TextBlock
                    Text="v1.0.0"
                    Foreground="{ThemeResource TextMutedBrush}"
                    FontSize="12"
                    VerticalAlignment="Center" />
            </StackPanel>
        </Grid>

        <!-- Navigation + content -->
        <controls:NavigationView
            x:Name="NavView"
            Grid.Row="1"
            PaneDisplayMode="Left"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            SelectionChanged="NavView_SelectionChanged"
            Background="{ThemeResource SidebarBrush}"
            OpenPaneLength="220">

            <controls:NavigationView.Resources>
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="{ThemeResource AppBackgroundColor}" />
                <SolidColorBrush x:Key="NavigationViewDefaultPaneBackground" Color="{ThemeResource SidebarColor}" />
            </controls:NavigationView.Resources>

            <controls:NavigationView.PaneHeader>
                <TextBlock
                    Text="Navigation"
                    Margin="16,12"
                    FontSize="12"
                    FontWeight="SemiBold"
                    Foreground="{ThemeResource TextMutedBrush}" />
            </controls:NavigationView.PaneHeader>

            <controls:NavigationView.MenuItems>
                <controls:NavigationViewItem Content="Home" Tag="Home">
                    <controls:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80F;" />
                    </controls:NavigationViewItem.Icon>
                </controls:NavigationViewItem>
                <controls:NavigationViewItem Content="Tool Catalog" Tag="Catalog">
                    <controls:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8F1;" />
                    </controls:NavigationViewItem.Icon>
                </controls:NavigationViewItem>
                <controls:NavigationViewItem Content="Downloads" Tag="Downloads">
                    <controls:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE896;" />
                    </controls:NavigationViewItem.Icon>
                </controls:NavigationViewItem>
            </controls:NavigationView.MenuItems>

            <controls:NavigationView.FooterMenuItems>
                <controls:NavigationViewItem Content="Settings" Tag="Settings">
                    <controls:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE713;" />
                    </controls:NavigationViewItem.Icon>
                </controls:NavigationViewItem>
            </controls:NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" Background="{ThemeResource AppBackgroundBrush}" />
        </controls:NavigationView>
    </Grid>
</Window>
